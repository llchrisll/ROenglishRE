TB_Layer_Priority_CLS = {
	Items_List = {
		-- I don't know exactly, what the all these flags (Default, Direction, etc) do
		-- [ACCESSORY_IDs_CLS.ACCESSORY_] = { Default = 304, isIgnoreRiding = true }, 
		-- [ACCESSORY_IDs_CLS.ACCESSORY_] = { Default = 143, Direction = {} },
	}
}